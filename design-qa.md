# Git Atlas 设计自审记录

## 结论

- 最终结果：**通过**
- 记录范围：**v1.0.0 至 v1.3.0**
- v1.3.0 阶段自审总分：**93.5 / 100**
- 硬门槛：总分不低于 85；实用性不低于 8.0 / 10；不得存在 P0、P1、P2 问题
- 当前问题等级：P0 = 0，P1 = 0，P2 = 0，P3 = 1

## 视觉真值与实现证据

- 视觉目标：`design/git-atlas-causal-field-final.png`
- 桌面实现：`design/implementation-final.png`
- 全屏并排比较：`design/design-qa-comparison-final.png`
- 因果场局部比较：`design/design-qa-focus-final.png`
- 成品图标提取：`design/packaged-icon.png`
- 用户问题截图：仅用于本地验收，不记录用户目录或临时文件路径
- 页签前后对照：`design/audit-v13-tabs-comparison.png`
- 单分支复核：`design/audit-v13-single-branch.png`
- v1.4.0 四模式成品：README 使用 Git Atlas 自身仓库重新生成的公开截图
- 视口：目标图 1487 × 1058；实现窗口 1440 × 1024，二者宽高比一致，比较图统一为 1440 × 1024
- 比较状态：中文深色界面、16 条提交、选中提交 `a7c2e18`、展开影响模块、右侧提交详情

## 评分

| 维度 | 权重 | 分数 | 审查结论 |
| --- | ---: | ---: | --- |
| 实用性 | 30% | 9.6 | 分支可达性、搜索、四模式分析、模块筛选、风险队列、真实对比与 Codex 分析形成闭环 |
| 信息密度 | 25% | 9.4 | 模式指标、专属工作区、提交表格、范围档案与详情面板同时保持可扫描 |
| 新颖度 | 20% | 9.1 | 用稳定拓扑配合模式化情报层；新颖性来自信息组织，不依赖扰动图形制造效果 |
| 清晰度 | 15% | 9.3 | 四模式拥有独立颜色、编号、大标题、用途说明、指标、列语义与操作区 |
| 性能可行性 | 10% | 9.0 | Canvas 拓扑、内存聚合、受限分支并发和可见性轮询；持续 CPU 极低 |

加权总分：`9.6×30% + 9.4×25% + 9.1×20% + 9.3×15% + 9.0×10% = 9.35 / 10`

## 迭代记录

| 版本 | 自评分 | 未通过原因 | 处理 |
| --- | ---: | --- | --- |
| v1 | 72 | 提交行背景遮住拓扑，核心可视化不可用（P1） | 调整 Canvas 图层与提交行背景关系 |
| v2 | 78 | 因果透镜可见，但拓扑分叉表达弱，仍像传统曲线图 | 重做选中点附近的局部变形与分支张力 |
| v3 | 82 | 只能稳定展示 12-13 条提交，信息密度低于产品目标 | 压缩行高并重排模块热度、变更规模和时间列 |
| v4 | 86 | 已跨总分线，但正文与辅助信息偏小，实用性只有 7.8 / 10 | 增大关键字号、提高次级文字对比度 |
| v5 | 88.2 | 视觉达标，但提交行内嵌套按钮触发渲染警告（P1） | 将提交行改为可键盘操作的 `role=button` 容器 |
| final | 89.9 | 无 P0/P1/P2 | 通过 |

## v1.1.0「跟随 Codex」专项自审

### 产品行为

- 默认关闭，必须由用户在左栏显式开启。
- 开启后，每 1.75 秒只读检查 Codex 当前选择的本地项目；仓库变化时才重新加载 Git 历史。
- 项目根目录本身是 Git 仓库时直接跟随；只有一个直属 Git 子仓库时跟随该仓库。
- 项目包含多个直属 Git 仓库时不猜测，保留当前仓库并给出中文歧义提示。
- 用户手动打开仓库后自动退出跟随；用户关闭开关后保持当时已经加载的仓库。
- 只读取 Codex 本机项目映射，不读取任务、聊天内容或代码内容。

### 自评分与迭代

| 版本 | 自评分 | 判定 | 发现的问题与处理 |
| --- | ---: | --- | --- |
| follow v1 | 84.3 | 不通过 | 每次轮询都会重复扫描子目录，日期仍为静态；加入项目签名缓存并改为动态本地日期 |
| follow v2 | 89.1 | 暂通过 | 开启与切换已验证，但“关闭后保持仓库”缺少显式自动化断言 |
| follow final | 91.2 | 通过 | 完成开启→自动切换→关闭并保持的闭环验证；修复便携版标识资源路径；运行时漏洞为 0 |

### 成品验证

- Windows 便携版：`Git-Atlas-1.1.0-Windows-x64.exe`，102,459,219 字节。
- SHA-256：`b4f35036f382cbdda8304bce319e11be4baed155b07d72147d461479014d99f2`。
- `codex-git-atlas` 场景：识别 Codex 项目、自动加载真实 Git、关闭后保持仓库；全部断言通过。
- 第二个真实仓库场景：从 Codex 项目映射定位仓库，读取真实提交与引用，开启/关闭闭环全部通过。
- 多仓库工作区场景：识别到多个候选仓库，显示歧义状态且不发生错误切换。
- 搜索、因果模式、提交选择、16 行密度、中文 UI：全部通过。
- Electron 渲染错误：0。
- `npm audit`：0 个已知漏洞。

## v1.2.0「可理解性与资源优化」专项自审

### 用户问题与处理

- 删除名不副实且难拖动的“语义缩放”滑块，改为大点击区的“列表密度：紧凑 / 标准 / 宽松”。
- 删除不基于仓库数据的“拓扑小图”装饰点，改为按真实提交时间聚合的“提交活跃度”；支持提示与点击定位。
- 补全两个可见但无行为的“比较父提交”按钮，改为真实 `git diff` 文件列表；根提交使用 `git diff-tree --root`。
- Codex 状态文件按修改时间缓存，轮询由 1.75 秒调整为 3 秒；窗口不可见时暂停，无状态变化时不触发 React 重绘。
- README 重写为中文产品页：明确价值主张、Git Atlas 自身仓库产品图、快速开始、界面读法、资源策略与路线图。

### 自评分与迭代

| 版本 | 自评分 | 判定 | 发现的问题与处理 |
| --- | ---: | --- | --- |
| v1.2 audit | 82.6 | 不通过 | 密度滑块难用、名称失真；小图为装饰数据；父提交比较是假按钮 |
| v1.2 candidate | 90.6 | 暂不通过 | 新交互通过，但根提交执行 `hash^` 比较失败 |
| v1.2 final | 92.8 | 通过 | 根提交回退补全；真实仓库成品包全部 11 项断言通过；渲染错误为 0 |

### 资源测量

测量环境：Windows x64、16 逻辑处理器、真实本地仓库、跟随 Codex 开启、Electron 44，均为便携版稳定后 15 秒采样。

- 可见空闲：工作集 367.7 MB，私有内存 320.3 MB，整机 CPU 约 0.202%。
- 最小化空闲：工作集 368.8 MB，私有内存 364.3 MB，整机 CPU 采样为 0%。
- 判定：持续 CPU 占用已足够低；Electron 基线内存仍不算轻量，列为 P3 持续优化项，不将其包装成“原生级轻量”。

### 成品验证

- Windows 便携版：`Git-Atlas-1.2.0-Windows-x64.exe`，102,462,545 字节。
- SHA-256：`f5d198c1a12b86d9def29e07f6edd137c5d3f03af5008950da485c2800484e5b`。
- 搜索、因果模式、提交选择、16 行密度、中文 UI：通过。
- 三档列表密度、真实提交活跃度、Codex 跟随闭环、父提交/根提交比较：通过。
- 真实测试仓库读取：19 条提交、11 个引用；通过。
- Electron 渲染错误：0。
- `npm audit`：0 个已知漏洞。

## v1.3.0「稳定拓扑与四模式工作台」专项自审

### 用户问题与根因

- 单独查看分支时图像不正常：旧实现只检查提交自身是否带分支引用，绝大多数可达提交没有引用标签，因此被错误丢弃。
- 点击节点后整张图改变：旧版在选中点前后 4 行重算横坐标，同时用展开行高推移下方所有节点，破坏了空间记忆。
- 四个模式差异不足：提交演化、因果场和模块影响主要共享同一列表，只有风险路径执行了数据过滤。
- 页面像玩具 Demo：主工作区缺少模式目的、范围指标、可操作聚合和小仓库的持续信息层。

### 处理

- 使用 `git rev-list` 计算本地分支的真实可达提交；最多处理 16 个分支，并限制 3 个 Git 子进程并发。
- 图谱节点使用全局稳定的泳道横坐标与固定行中心；选中节点只改变光环、关系强调和详情，不再改变基础几何。
- 提交演化、因果场、模块影响、风险路径拥有独立的大标题、模式编号、目的说明、关键指标、工作区、列语义和数据行为。
- 因果场计算完整祖先/后继集合；模块模式支持真实模块筛选；风险模式生成评分并维护优先队列。
- 列表后增加“当前范围档案”，聚合模块、贡献者和质量信号，并对小仓库自适应填充剩余工作区。

### 自评分与迭代

| 版本 | 自评分 | 判定 | 发现的问题与处理 |
| --- | ---: | --- | --- |
| v1.3 audit | 76.4 | 不通过 | 分支过滤破坏拓扑；点击导致全图漂移；模式仅是页签高亮 |
| v1.3 workspace | 86.0 | 不通过 | 四模式功能已分离，但因果卡片截断偏多，小仓库下方仍显空 |
| v1.3 refined | 92.0 | 暂通过 | 路径卡片改为纵向可读结构，范围档案自适应高度，补齐焦点可见性 |
| v1.3 final | 93.5 | 通过 | 便携版 16 项自动断言通过；真实单分支 13 个可达提交复核通过；渲染错误为 0 |

### 资源测量

测量环境：Windows x64、16 逻辑处理器、真实本地仓库、跟随 Codex 开启、Electron 44，便携版稳定后采样。

- 可见空闲：4 个进程，工作集 360.3 MB，私有内存 294.3 MB，整机 CPU 约 0.02%。
- 最小化空闲：4 个进程，工作集 352.6 MB，私有内存 283.9 MB，整机 CPU 采样为 0%。
- 与 v1.2.0 同环境记录相比，可见工作集下降约 7.4 MB，私有内存下降约 26.0 MB，持续 CPU 从约 0.202% 降至约 0.02%。
- 判定：新聚合层没有引入持续计算；Electron 基线内存仍是 P3 约束，继续如实说明。

### 成品验证

- Windows 便携版：`Git-Atlas-1.3.0-Windows-x64.exe`，102,467,073 字节。
- SHA-256：`ca73fc39b5b919be7788d63917e4a9001c584e519f29c9fcba56e3e7ce64437e`。
- 搜索、提交选择、稳定拓扑、分支可达性、三档密度、真实活跃度：通过。
- 四模式标题与专属工作区、因果路径、模块筛选、风险队列：通过。
- Codex 跟随开启→切换→关闭保持、父提交/根提交比较：通过。
- 真实单分支场景：显示 13 个可达提交，与 `git rev-list` 一致。
- Electron 渲染错误：0；`npm audit`：0 个已知漏洞。

## 可见差异判断

### 字体与层级

- 实现使用系统中文字体栈，避免打包字体依赖；标题、提交信息、哈希、增删数字和辅助信息的层级与目标一致。
- 实现中的标签和微型图例略大于目标图，这是为了 1440 × 1024 桌面窗口下的可读性，属于有意调整。

### 布局与信息顺序

- 左侧仓库/分支/标签/真实提交活跃度、中部模式工作区与提交历史、右侧详情构成稳定三栏结构。
- 目标图把变更数字放在拓扑左侧；实现把拓扑保持为连续第一列，再把模块热度与变更规模相邻放置。这样扫描提交信息时不会被数字切断拓扑，属于实用性优先的有意调整。

### 颜色与资产

- 深色基底、蓝色选中态、绿色新增、红色删除、紫/蓝分支语义与目标一致。
- 图标全部来自 Phosphor 图标库，没有使用字符占位、手绘 SVG 或伪造资产。

### 因果场效果

- v1.3.0 主动移除了会改变邻近节点横坐标和下方行位置的“局部拓扑扩张”。基础 DAG 永久稳定，选中态仅使用焦点光晕与路径强调。
- 新颖表达转移到独立因果工作区：上游祖先、当前焦点、下游后继和关系化列表共同解释因果，同时保留用户的空间记忆。

## 功能验证

- TypeScript 类型检查：通过
- Vite 生产构建：通过
- Windows 便携版封装：通过；`Git-Atlas-1.3.0-Windows-x64.exe`，102,467,073 字节
- 成品启动退出码：0
- 真实仓库 Git 读取：通过；读取到当前分支、提交和引用
- 搜索过滤：通过
- 四模式专属工作区、模式标题与“只看关联路径”：通过
- 单分支可达性与节点点击拓扑稳定性：通过
- 模块筛选与风险队列：通过
- 提交选择和详情同步：通过
- 16 行高密度布局：通过
- 中文 UI 关键文案：通过
- 渲染层错误：0
- 正式应用图标：已写入便携版并从最终可执行文件反向提取验证

## 最终判定

这版不是传统 Git 泳道图的换肤，也没有用图形重排制造“高级感”。新颖性来自稳定拓扑之上的四种仓库情报工作区，实用性来自真实分支可达性、可操作聚合、高密度表格与提交详情。满足交付门槛。
