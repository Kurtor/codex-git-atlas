# Git Atlas 设计自审记录

## 结论

- 最终结果：**通过**
- 自审总分：**89.9 / 100**
- 硬门槛：总分不低于 85；实用性不低于 8.0 / 10；不得存在 P0、P1、P2 问题
- 当前问题等级：P0 = 0，P1 = 0，P2 = 0，P3 = 1

## 视觉真值与实现证据

- 视觉目标：`design/git-atlas-causal-field-final.png`
- 桌面实现：`design/implementation-final.png`
- 全屏并排比较：`design/design-qa-comparison-final.png`
- 因果场局部比较：`design/design-qa-focus-final.png`
- 成品图标提取：`design/packaged-icon.png`
- 视口：目标图 1487 × 1058；实现窗口 1440 × 1024，二者宽高比一致，比较图统一为 1440 × 1024
- 比较状态：中文深色界面、16 条提交、选中提交 `a7c2e18`、展开影响模块、右侧提交详情

## 评分

| 维度 | 权重 | 分数 | 审查结论 |
| --- | ---: | ---: | --- |
| 实用性 | 30% | 9.2 | 本地仓库选择、真实 Git 读取、搜索、筛选、提交详情、Codex 分析均位于主工作流内 |
| 信息密度 | 25% | 9.3 | 单屏保留 16 条提交、拓扑、热度、变更规模、作者、时间、模块影响与风险信息 |
| 新颖度 | 20% | 8.6 | 用“可变形因果场”替代传统固定泳道，选中提交时局部拓扑展开并形成因果透镜 |
| 清晰度 | 15% | 8.7 | 视觉层级稳定，选中态、分支色、增删色、风险色和模块条均有明确语义 |
| 性能可行性 | 10% | 8.8 | Canvas 2D 绘制拓扑，DOM 负责高密度数据，500 条提交读取上限避免无界载入 |

加权总分：`9.2×30% + 9.3×25% + 8.6×20% + 8.7×15% + 8.8×10% = 8.99 / 10`

## 迭代记录

| 版本 | 自评分 | 未通过原因 | 处理 |
| --- | ---: | --- | --- |
| v1 | 72 | 提交行背景遮住拓扑，核心可视化不可用（P1） | 调整 Canvas 图层与提交行背景关系 |
| v2 | 78 | 因果透镜可见，但拓扑分叉表达弱，仍像传统曲线图 | 重做选中点附近的局部变形与分支张力 |
| v3 | 82 | 只能稳定展示 12–13 条提交，信息密度低于产品目标 | 压缩行高并重排模块热度、变更规模和时间列 |
| v4 | 86 | 已跨总分线，但正文与辅助信息偏小，实用性只有 7.8 / 10 | 增大关键字号、提高次级文字对比度 |
| v5 | 88.2 | 视觉达标，但提交行内嵌套按钮触发渲染警告（P1） | 将提交行改为可键盘操作的 `role=button` 容器 |
| final | 89.9 | 无 P0/P1/P2 | 通过 |

## 可见差异判断

### 字体与层级

- 实现使用系统中文字体栈，避免打包字体依赖；标题、提交信息、哈希、增删数字和辅助信息的层级与目标一致。
- 实现中的标签和微型图例略大于目标图，这是为了 1440 × 1024 桌面窗口下的可读性，属于有意调整。

### 布局与信息顺序

- 左侧仓库/分支/标签/拓扑小图、中部提交历史、右侧详情三栏结构与目标一致。
- 目标图把变更数字放在拓扑左侧；实现把拓扑保持为连续第一列，再把模块热度与变更规模相邻放置。这样扫描提交信息时不会被数字切断拓扑，属于实用性优先的有意调整。

### 颜色与资产

- 深色基底、蓝色选中态、绿色新增、红色删除、紫/蓝分支语义与目标一致。
- 图标全部来自 Phosphor 图标库，没有使用字符占位、手绘 SVG 或伪造资产。

### 因果场效果

- 实现保留目标中的局部拓扑扩张、聚焦光晕、分支折射、因果路径减淡和选中态展开。
- P3：目标图的透镜边缘更具雕塑感；实现刻意降低折射与发光强度，以保持文字清晰和长历史滚动时的性能。该差异不影响核心体验。

## 功能验证

- TypeScript 类型检查：通过
- Vite 生产构建：通过
- Windows 便携版封装：通过；`Git-Atlas-1.0.0-Windows-x64.exe`，88,049,434 字节
- 成品启动退出码：0
- 真实仓库 Git 读取：通过；读取到当前分支、提交和引用
- 搜索过滤：通过
- 模式切换与“只看关联路径”：通过
- 提交选择和详情同步：通过
- 16 行高密度布局：通过
- 中文 UI 关键文案：通过
- 渲染层错误：0
- 正式应用图标：已写入便携版并从最终可执行文件反向提取验证

## 最终判定

这版不是传统 Git 泳道图的换肤，也没有为了炫技牺牲信息密度。新颖性来自对局部因果关系的可变形表达，实用性来自真实 Git 数据、稳定的高密度表格和可操作的提交详情。满足交付门槛。
